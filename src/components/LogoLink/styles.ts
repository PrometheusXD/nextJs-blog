import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  color: inherit;

  > img {
    width: 15rem;
    height: 15rem;
    border-radius: 50%;
  }
`;
